{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "System": "Information",
      // Avoid logging lifetime events unless Error
      "Microsoft.Hosting.Lifetime": "Information"
    }
  }
}